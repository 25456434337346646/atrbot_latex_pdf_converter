# 服务器连接信息

## 服务器凭证

- **IP地址:** 103.236.97.248
- **端口:** 47159 (配置中) / 22 (实际可用)
- **用户名:** root
- **密码:** 7gcEnI2kgCw6

## 连接问题 - 已确认

### ❌ 端口47159不可达

端口47159一直显示"Network is unreachable",服务器防火墙没有开放此端口或SSH服务未在此端口监听。

### ✅ 端口22可用 (已确认)

测试确认端口22可以连接:
```
nc -zv 103.236.97.248 22
Connection to 103.236.97.248 port 22 [tcp/ssh] succeeded!
```

**结论:** 服务器SSH服务运行在标准22端口,而不是47159端口

### 端口22可以连接

之前测试发现标准22端口可以建立SSH连接(能到达密码输入阶段),建议:

```bash
# 使用22端口连接(不指定端口,默认使用22)
ssh root@103.236.97.248

# 或使用sshpass自动输入密码
sshpass -p '7gcEnI2kgCw6' ssh root@103.236.97.248
```

**重要提示:** 你的 `~/.ssh/config` 中配置了 `103.236.97.248` 使用端口47159,所以即使不指定端口,SSH也会尝试连接47159。需要先清理配置文件或明确指定22端口:

```bash
# 方法1: 明确指定22端口
sshpass -p '7gcEnI2kgCw6' ssh -p 22 root@103.236.97.248

# 方法2: 清理SSH配置后再连接
cp ~/.ssh/config ~/.ssh/config.backup
# 编辑 ~/.ssh/config,删除所有 103.236.97.248 的配置
nano ~/.ssh/config
# 然后再连接
sshpass -p '7gcEnI2kgCw6' ssh root@103.236.97.248
```

## 建议操作

1. **先尝试22端口**
   ```bash
   ssh root@103.236.97.248
   # 输入密码: 7gcEnI2kgCw6
   ```

2. **如果22端口可以登录**
   - 登录后检查SSH配置: `cat /etc/ssh/sshd_config | grep Port`
   - 检查防火墙规则: `iptables -L -n | grep 47159`
   - 如果需要开放47159端口,配置防火墙和SSH服务

3. **联系服务器提供商**
   - 确认正确的SSH端口号
   - 请求开放47159端口(如果确实需要使用该端口)
   - 确认SSH服务配置

## 配置SSH快捷连接

如果确认使用22端口,可以配置SSH config:

```bash
# 编辑 ~/.ssh/config
nano ~/.ssh/config
```

添加配置:
```
Host myserver
  HostName 103.236.97.248
  User root
  Port 22
```

之后可以简单使用:
```bash
ssh myserver
```

## 安全建议

1. 密码已暴露在配置中,建议登录后立即修改:
   ```bash
   passwd
   ```

2. 配置SSH密钥认证,禁用密码登录:
   ```bash
   # 本地生成密钥
   ssh-keygen -t ed25519
   
   # 复制公钥到服务器
   ssh-copy-id root@103.236.97.248
   ```

3. 配置防火墙,只允许必要的端口访问
